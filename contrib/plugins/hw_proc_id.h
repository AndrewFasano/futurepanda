#ifndef HW_PROC_ID_H
#define HW_PROC_ID_H

/*
 * Prototypes for the do_add and do_sub functions. Both return an int and
 * take an int as an argument.
 */
QPP_FUN_PROTOTYPE(hw_proc_id, uint64_t, hw_proc_id_get, void);

#endif /* HW_PROC_ID_H */
